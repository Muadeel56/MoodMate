// Export all theme-related utilities and configurations
export { default as themeConfig, themeClasses, gradients, generateCSSVariables, getThemeColor } from './theme';

// Export global styles
export { default as globalStyles } from './globals.css'; 